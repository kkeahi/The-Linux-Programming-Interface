Aside from the absence of error checking and various variable and structure
 declarations, what is the problem with the following program?
 static void *
 threadFunc(void *arg)
 {
  struct someStruct *pbuf = (struct someStruct *) arg;
  /* Do some work with structure pointed to by 'pbuf' */
 }
 int
 main(int argc, char *argv[])
 {
  struct someStruct buf;
  pthread_create(&thr, NULL, threadFunc, (void *) &buf);
  pthread_exit(NULL);
 }



buf is defined locally in the stack of the main thread.
when the main thread terminates (pthread_exit()), buf,
which is passed into the created thread, points to a
destroyed stack frame, and is therefore dangling.